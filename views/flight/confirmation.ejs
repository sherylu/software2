<%- include ../partials/banner.ejs %>

<div class="col-md-8">
  <div class="jumbotron">
      <h1>Reserva Exitosa</h1>
      <p>Su reserva ha sido registrada exitosamente en el sistema</p>
      <p><b>Codigo de Vuelo: </b>
        <ul>
          <% _.each(fligthCodes, function(code) { %>
            <li><%=code%></li>  
          <% }) %>
        </ul>
      </p>
      <p>
      <b>Codigos de Reserva: </b>
        <ul>
          <%for (var i=0; i<reservations.length; i++) {%>
            <li><b>Codigo: </b><%=reservations[i].code%> <b> Nombre: </b><%=clients[i].firstName + clients[i].lastName%></li>
          <% }; %>
        </ul>
      </p>
      <p><a class="btn btn-primary btn-lg" href="/" role="button">Regresar al inicio</a></p>
    </div>
</div>